import { HighlightFoundDirective } from './highlight-found.directive';

describe('HighlightFoundDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightFoundDirective();
    expect(directive).toBeTruthy();
  });
});
